# VectorBrain Python Application
# Install: pip install -r requirements.txt
# Milestone 0-2: core server + world model + voice
# ML perception deps (audio, vision, embeddings) added at their milestone

# ── Server ──────────────────────────────────────────────────────────────────
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
httpx>=0.27.0          # async HTTP client for calling Go API
pydantic>=2.10.0

# ── LLM (Ollama-compatible via OpenAI client) ─────────────────────────────
openai>=1.60.0

# ── Utilities ────────────────────────────────────────────────────────────
python-dotenv>=1.0.0
loguru>=0.7.2          # structured logging with context binding

# ── Testing ──────────────────────────────────────────────────────────────
pytest>=8.0.0
pytest-asyncio>=0.24.0
httpx>=0.27.0          # also used for TestClient

# ── Type checking (optional, run: mypy vectorbrain/) ─────────────────────
# mypy>=1.8.0
